#include "main.h"

/**
 * _memcpy - a function that copies memory area
 * @dest: destination of memory area to copy
 * @src: memory area to copy from
 * @n: number of bytes
 * Return: returns a pointer to copy location
 */

char *_memcpy(char *dest, char *src, unsigned int n)
{
	char *start = dest;

	while (i--)
	{
		*dest = *src;
		src++;
		dest++;
	}
	return (start);
}
