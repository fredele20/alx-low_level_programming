#include "main.h"

/**
 *_abs - prints the absolute value of a number
 * @r: An integre input
 * Return: Absolut value of number r
 */

int _abs(int r)
{

	if (r >= 0)
		return (r);
	else
		return (r);

}
